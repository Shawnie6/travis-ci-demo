language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t shawnie86/travis-demo -f Dockerfile.dev .

script:
  - docker run -e CI=true shawnie86/travis-demo npm run test -- --coverage
